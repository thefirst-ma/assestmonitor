# Claude Guide

## 项目核心目标

本项目的核心目标是长期股票投研推荐。

它不是短线择时工具，而是一个可解释、可复盘、可持续迭代的长期推荐系统。系统每天生成值得推荐或重点观察的股票，并通过 Telegram 通知用户。推荐结果按三个阶段输出：

- 月度推荐：关注趋势、回撤、短期风险和催化。
- 季度推荐：关注行业景气、基本面改善和估值变化。
- 年度推荐：关注企业护城河、负责人质量、行业长期趋势、政策影响和资本配置。

每条推荐都必须展示推荐因子，包括：

- 因子名称。
- 因子描述。
- 权重。
- 个股评分。
- 对总分的贡献。
- 推荐理由和风险。

系统还必须支持历史推荐复盘。用户需要能回看每次推荐，判断推荐是否准确、为什么准确、为什么不准确，并把复盘原因用于后续优化推荐因子和权重。

重要判断原则：历史数据不能完全拟合未来。推荐应同时考虑历史价格和财务表现、企业当前状态、负责人特点、未来行业趋势、政策因素、用户自定义因子和人工研究档案。

## 数据库和 SQL 约定

所有建表语句、迁移语句、数据修复语句、初始化数据和数据库操作语句，必须统一生成到 `sql/` 文件夹下。

数据库相关工作必须遵守：

- 不要只在代码中隐式创建或修改表结构。
- 每次涉及数据库结构或数据初始化，都要新增或更新 `sql/` 下的 SQL 文件。
- SQL 文件名使用递增编号加说明，例如 `002_market_data_sources_mysql.sql`。
- MySQL 脚本默认面向 MySQL 8+。
- 表名和字段名使用 snake_case。
- 外部数据必须记录来源，例如 `source_id`、`source_symbol`、`raw_json`。
- 推荐历史必须保存当次推荐快照，包括当时使用的因子、权重、评分和贡献。
- 复盘结果必须能追溯到 `run_id + symbol + horizon`。

当前已有 SQL：

- `sql/001_core_recommendation_mysql.sql`
- `sql/002_market_data_sources_mysql.sql`
- `sql/003_app_runtime_mysql.sql`

## 数据源规划

项目需要支持多个股票数据源：

- Yahoo Finance：当前已有，主要用于美股和全球股票。
- AKShare：优先用于 A 股历史行情和基础数据。
- BaoStock：优先用于 A 股历史 K 线。
- TuShare：用于更专业的财务、指数和宏观数据，需要 token 时通过环境变量配置。
- 同花顺：可以作为候选源，但必须确认稳定性、合法性和长期可维护性后再启用。

后续新增数据源时，应新增 provider/adapter，不要把推荐逻辑直接绑定到某一个外部网站。
