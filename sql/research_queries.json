{
  "latestSnapshot": "SELECT r.id AS run_id, r.generated_at, r.source, i.horizon, i.payload_json FROM recommendation_runs r LEFT JOIN recommendation_items i ON i.run_id = r.id WHERE r.id = (SELECT id FROM recommendation_runs ORDER BY generated_at DESC, id DESC LIMIT 1) ORDER BY i.horizon, i.score DESC"
}
