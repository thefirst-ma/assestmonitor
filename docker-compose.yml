version: '3.8'

services:
  crypto-monitor:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
