# Agent Guide

## 项目核心目标

本项目的核心目标是构建一个长期股票投研推荐系统，而不是短线交易信号系统。

系统需要每天生成最值得长期关注的股票，并通过 Telegram 通知用户。推荐应分为三个周期：

- 按月：偏趋势、回撤、短中期催化和风险扰动。
- 按季度：偏行业景气、基本面改善、财务质量和估值变化。
- 按年：偏企业护城河、负责人质量、长期行业趋势、政策影响和资本配置能力。

推荐结果必须解释原因，展示推荐因子、因子描述、因子权重、个股因子评分和当次推荐中的因子贡献。系统还必须支持回看历史推荐，判断每次推荐是否准确、为什么准确、为什么不准确，并把复盘结论沉淀为后续优化推荐策略的依据。

历史数据不能被视为对未来的完全拟合。系统应把历史价格、财务数据、企业当前情况、负责人特点、行业趋势、政策因素、用户自定义因子和人工研究档案结合起来，形成可追踪、可复盘、可调整的推荐流程。

## 数据库和 SQL 约定

所有建表语句、迁移语句、数据修复语句、初始化数据和数据库操作脚本，必须统一生成到 `sql/` 目录下。

新增或修改数据库结构时，必须遵守：

- 不要只把数据库结构写在应用代码里。
- 必须新增或更新 `sql/` 下的 SQL 文件。
- 文件名使用递增编号和清晰含义，例如 `001_core_recommendation_mysql.sql`。
- MySQL 相关脚本优先使用 MySQL 8+ 语法。
- 表和字段命名使用 snake_case。
- 时间字段优先使用 Unix 秒级时间戳，字段名使用 `created_at`、`updated_at`、`generated_at`、`reviewed_at` 等。
- 外部数据源采集结果必须保留 `source_id` 和必要的 `raw_json`，便于追溯。
- 推荐历史必须保存当次推荐快照，不能只保存当前最新因子配置。

当前 SQL 文件：

- `sql/001_core_recommendation_mysql.sql`：推荐系统、推荐因子、推荐历史、复盘相关表。
- `sql/002_market_data_sources_mysql.sql`：行情数据源、股票主数据、历史价格、财务指标、公司信息、行业映射和同步任务表。
- `sql/003_app_runtime_mysql.sql`：登录用户、候选资产、监控价格等应用运行时表。

## 数据源方向

系统应支持多数据源适配，而不是绑定单一网站：

- Yahoo Finance：美股和全球股票基础行情、财务指标。
- AKShare：A 股历史行情、基础数据。
- BaoStock：A 股历史 K 线。
- TuShare：专业财务、行情、指数、宏观数据，按 token 权限启用。
- 同花顺：作为候选数据源，仅在确认接口稳定、合法、可长期使用后启用。

应用推荐服务应优先读取统一后的本地数据表，再按需要触发外部数据同步。
